pipeline {
  

  agent { label 'master' }

   stage("cloning from the GIT") {
	steps {
	echo "clonng from git "
	}


   }

   stage("build using maven")
   {
	steps {
	echo "build using maven"
	      }}		
   }

   stage("results")
   {
	steps {
	echo "results stage"
		}
   }


  } 
